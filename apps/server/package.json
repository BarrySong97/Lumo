{
  "name": "@lumo/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run  --hot src/index.ts",
    "build": "bun build --compile --minify src/index.ts --outfile dist/server",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lumo/api": "workspace:*",
    "@lumo/db": "workspace:*",
    "@orpc/server": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}